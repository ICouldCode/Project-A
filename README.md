# Project A

## Build on macOS

Install raylib with Homebrew if you do not already have it:

```sh
brew install raylib
```

Configure and build:

```sh
cmake -S . -B build
cmake --build build
```

Run the game:

```sh
./build/MyRaylibGame
```
